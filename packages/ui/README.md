# packages/ui

Spazio riservato all'estrazione dei componenti UI condivisibili tra web e desktop.

In questa fase i componenti restano in `apps/web` per evitare un refactor grafico invasivo durante la pulizia strutturale.
