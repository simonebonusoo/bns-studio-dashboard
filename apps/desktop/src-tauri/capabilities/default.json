{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capacità di base per la finestra principale di BNS Studio OS",
  "windows": ["main"],
  "permissions": ["core:default"]
}
